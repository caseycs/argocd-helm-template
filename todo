end-to-end tests for all use-cases and flags

+ support help flags:
+    releaseName: karpenter
+    skipCrds: true

support applications without values

support dynamic values path: treat current repo as first `ref:`, allow to overwrite local repo path by ref name

support mutliple valueFiles

support
    parameters:
        - name: app
          value: $ARGOCD_APP_NAME


bug with git repo when new tag was pushed

support multiple sources (helm+raw declarations)

support local chart development (use local copy instead of git clone, repo registry or OCI pull)

support additional declarations (useful when chars does not support extra resources)